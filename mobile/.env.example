# 🔒 API Key Setup - FoodHealth App

## Sicherheitshinweis
Die `.env` Datei enthält deine API Keys und wird **NICHT** zu GitHub hochgeladen.

## Setup für neue Entwickler

1. **Kopiere `.env.example` zu `.env`:**
   ```bash
   cp .env.example .env
   ```

2. **Füge deinen OpenAI API Key ein:**
   - Gehe zu [platform.openai.com](https://platform.openai.com)
   - Erstelle einen neuen API Key
   - Füge ihn in die `.env` Datei ein:
   ```
   EXPO_PUBLIC_OPENAI_API_KEY=dein_echter_api_key_hier
   ```

3. **Starte die App:**
   ```bash
   npx expo start
   ```

## Wichtig
- ⚠️ **NIEMALS** API Keys direkt im Code schreiben
- ✅ **IMMER** über `.env` Dateien laden
- 🔒 `.env` ist in `.gitignore` und wird nicht committed